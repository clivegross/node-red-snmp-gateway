{
    "name": "snmp-interface",
    "description": "A middleware project to SNMP poll and transform to data other interfaces",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}